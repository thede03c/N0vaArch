#!/usr/bin/env bash
set -euo pipefail

if command -v systemsettings >/dev/null 2>&1; then
  exec systemsettings
fi
if command -v systemsettings6 >/dev/null 2>&1; then
  exec systemsettings6
fi
echo "systemsettings not found." >&2
exit 1
