#!/usr/bin/env bash
set -euo pipefail

exec /usr/local/bin/novaarch-terminal --title "NovaArch Updater" -- sh -lc '
  echo "NovaArch update session"
  echo "----------------------"
  sudo pacman -Syu
  echo
  echo "Press Enter to close..."
  read -r _
'
