Place your custom branding assets here:

- `logo.svg` (distribution logo)
- `splash.png` (optional wallpaper/splash image)

`build-iso.sh` will copy them into the live filesystem automatically when present.
